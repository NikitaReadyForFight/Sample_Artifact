name: Google Test Example

on:
  workflow_dispatch:
    

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Install Google Test
        uses: ./for_action/.github/actions/Gtest_installation/action.yml
